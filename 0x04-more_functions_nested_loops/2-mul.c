#include "main.h"
/**
 * mul - our function to multiply two integers
 * @a: as our first integer to be used
 * @b: as our second integer to be used
 *
 * Return: always to value of result (r).
 */
int mul(int a, int b)
{
	int r;

	r = a * b;
	return (r);
}
